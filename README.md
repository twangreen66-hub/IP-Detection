# IP-Detection
安卓IP檢測，這是一個標準的 Android (Kotlin + Jetpack Compose) 專案
由于这是一个标准的 Android (Kotlin + Jetpack Compose) 专案 ，下载完代码后，请依以下步骤在本地端执行：

安裝 Android Studio：
安装 Android Studio ：

請確保電腦上已安裝最新版本的 Android Studio（推薦 Koala 或更新版本）。
请确保电脑上已安装最新版本的 Android Studio （推荐 Koala 或更新版本）。匯入專案 (Open Project)：
汇入专案 (Open Project) ：

開啟 Android Studio，選擇 「Open」，然後選取剛剛解壓縮（或從 Git 克隆下來）的專案根目錄。
开启 Android Studio，选择 「Open」 ，然后选取刚刚解压缩（或从 Git 克隆下来）的专案根目录。

自動同步 Gradle (Gradle Sync)：
自动电影 Gradle（Gradle Sync） ：

Android Studio 會自動識別並載入設定檔（build.gradle.kts）。
Android Studio 会自动电影并加载设定档( build.gradle.kts )。

專案內建的依賴套件（包含 Room 資料庫、Retrofit 網路流、Moshi 解析、與 Leaflet 動態地圖 WebView 等）將會自動下載並同步完成。
专案内建的依赖套件（包含 Room 资料库、Retrofit 网路流、Moshi 解析、与 Leaflet 动态地图 WebView 等）将会自动下载并同步完成。執行應用程式：
执行应用程式 ：

連接您的 Android 實體手機（開啟 USB 偵錯），或是啟動 Android Studio 內建的虛擬機（Emulator）。
连接您的 Android 实体手机（开启 USB 侦错），或是启动 Android Studio 内建的虚拟机（Emulator）。
點選上方工具列的 綠色「Run」按鈕（或快捷鍵 Shift + F10），系統即會自動編譯並將 APK 安裝到設備上執行。
点选上方工具列的绿色「Run」按钮 （或快捷键 Shift + F10 ），系统即会自动编译并将 APK 安装到设备上执行。
